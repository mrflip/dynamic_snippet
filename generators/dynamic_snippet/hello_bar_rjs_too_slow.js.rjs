# Replace the contents of the #hello_bar div with the "hello friend"-or-"sign in" greeting
# This allows page caching (and so enormous salutary performance gains)
page['hello-bar'].replace_html :partial => 'users/hello_bar'
